services:
  api:
    build:
      context: ..
    env_file:
      - ../src/.env    

  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
